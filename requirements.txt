# 视频转录工具依赖 (LangChain 1.0)

# MCP服务器
mcp>=1.0.0

# LangChain 1.0 核心 (仅使用 langchain-core，不依赖旧版 langchain)
langchain-core>=0.3.0

# ASR (FunASR + ModelScope)
funasr>=1.0.0
modelscope>=1.10.0
dashscope>=1.20.0

# 音频处理
soundfile>=0.12.0
librosa>=0.10.0

# 向量数据库
chromadb>=0.5.0
sentence-transformers>=3.0.0

# Jupyter Notebook处理
nbconvert>=7.0.0
nbformat>=5.0.0

# 文档解析
python-markdown>=3.5.0
beautifulsoup4>=4.12.0

# 数据处理
pydantic>=2.0.0
pyyaml>=6.0.0
numpy>=1.24.0
tqdm>=4.65.0

# 音频处理
pydub>=0.25.0

# 工具
python-dotenv>=1.0.0
